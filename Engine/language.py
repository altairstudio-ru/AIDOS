def normalize(text: str) -> str:
    """
    MVP: пока без AI перевода.
    В будущем сюда подключится LLM.
    """

    # временно: просто пропускаем как есть
    # (позже заменится на реальную модель перевода)
    return text.strip()
